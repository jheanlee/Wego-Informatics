#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

int main(int argc, char *argv[]) {
  registerValidation(argc, argv);
  inf.readInt(1, 30, "x");
  inf.readSpace();
  inf.readInt(1, 1000, "y");
  inf.readEoln();
  inf.readEof();
}