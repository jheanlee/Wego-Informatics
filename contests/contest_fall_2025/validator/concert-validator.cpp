#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

int main(int argc, char *argv[]) {
  registerValidation(argc, argv);
  inf.readInt(0, 200000, "k");
  inf.readEoln();
  inf.readInt(0, 200000, "a");
  inf.readSpace();
  inf.readInt(0, 200000, "b");
  inf.readEoln();
  inf.readEof();
}