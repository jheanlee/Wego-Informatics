#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

int main(int argc, char *argv[]) {
  registerValidation(argc, argv);
  inf.readInt(1, 1000, "w_1");
  inf.readSpace();
  inf.readInt(1, 1000, "w_2");
  inf.readEoln();
  inf.readInt(1, 3000, "w_l");
  inf.readEoln();
  inf.readEof();
}
